{
  "hash": "3e2243b7364347f1c1b5c12aa5cafab4",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"Dimension reduction\"\nauthor: \"Dr. Alexander Fisher\"\nexecute:\n  warning: true\nformat: \n    revealjs:\n      smaller: true\n---\n\n\n\n\n\n# Background\n\nThe tidy way of thinking: rows are observations, columns are measurements.\n\nDimension reduction is the art of replacing many columns with a few carefully chosen (or constructed) columns.\n\n<!-- . . . -->\n\n<!-- A sketch of today's lecture: -->\n\n<!-- - covariance matrices can be visualized as ellipses -->\n\n<!-- - eigenvectors point along the ellipse's main axes -->\n\n<!-- - PCA reduces dimension by keeping the most important axes (linear combinations of existing columns) -->\n\n<!-- - MDS is another form of dimension reduction -->\n\n## SNP Data Example\n\n\n\n\n\n::: {.cell}\n\n:::\n\n\n\n\n\n<!-- ```{r} -->\n<!-- #| echo: false -->\n<!-- snp_data |> -->\n<!--   as.data.frame() |> -->\n<!--   head(n = 5) -->\n<!-- ``` -->\n\n![](images/01-welcome/alignment.png)\n\nSNP data typically record, for many individuals and many SNP locations, how many copies of a particular allele each person carries. Because humans have two copies of most chromosomes, a SNP is encoded as 0, 1, or 2 copies of the allele.\n\n<!-- The human genome is the complete set of genetic information contained in our DNA. It consists of roughly 3 billion DNA base pairs, built from four nucleotide bases—A, C, G, and T—organized into 23 pairs of chromosomes. -->\n\n<!-- Most of the human genome is shared across individuals, but there are millions of locations where people commonly differ. One of the most common types of genetic variation is a single nucleotide polymorphism (SNP, pronounced \"snip\"), where a single DNA base differs between individuals. For example, at a particular location in the genome, some people might have an A while others have a G. -->\n\n. . .\n\nHow to visualize?\n\n## \n\n## Matrices in R\n\n::::: columns\n::: {.column width=\"50%\"}\n#### Make matrices\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nA <- matrix(c(1, 2, 2, 1), nrow = 2)\nA\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n     [,1] [,2]\n[1,]    1    2\n[2,]    2    1\n```\n\n\n:::\n\n```{.r .cell-code}\nB <- matrix(c(0, 1, 0, 1), nrow = 2)\nB\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n     [,1] [,2]\n[1,]    0    0\n[2,]    1    1\n```\n\n\n:::\n:::\n\n\n\n\n:::\n\n::: {.column width=\"50%\"}\n#### Multiply a matrix\n\nMatrix multiplication proceeds row by column.\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nA %*% B # multiplication\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n     [,1] [,2]\n[1,]    2    2\n[2,]    1    1\n```\n\n\n:::\n:::\n\n\n\n\n:::\n:::::\n\n. . .\n\nThe single entry in row 1, column 1 is just a dot product:\n\n$$\\require{enclose}\n\\left[\n\\begin{array}{c}\n\\enclose{circle}[mathcolor=\"red\",padding=\"6px\"]{\\begin{array}{cc}1 & 2\\end{array}}\\\\\n\\begin{array}{cc}2 & 1\\end{array}\n\\end{array}\n\\right]\n\\left[\n\\begin{array}{cc}\n\\enclose{circle}[mathcolor=\"red\",padding=\"6px\"]{\\begin{array}{c}0\\\\1\\end{array}} &\n\\begin{array}{c}0\\\\1\\end{array}\n\\end{array}\n\\right]\n=\n\\left[\n\\begin{array}{cc}\n\\enclose{circle}[mathcolor=\"red\",padding=\"8px\"]{1(0)+2(1)} & 1(0)+2(1)\\\\\n2(0)+1(1) & 2(0)+1(1)\n\\end{array}\n\\right]$$\n\nSame rule for every entry: choose a row, choose a column, multiply across, add up.\n\n## Matrix vector multiplication\n\nA vector is just a matrix with one column.\n\n$$\\require{enclose}\n\\left[\n\\begin{array}{cc}\n3 & 1\\\\\n1 & 2\n\\end{array}\n\\right]\n\\left[\n\\enclose{circle}[mathcolor=\"red\",padding=\"6px\"]{\\begin{array}{c}2\\\\1\\end{array}}\n\\right]\n=\n\\left[\n\\begin{array}{c}\n3(2)+1(1)\\\\\n1(2)+2(1)\n\\end{array}\n\\right]\n=\n\\left[\n\\begin{array}{c}\n7\\\\\n4\n\\end{array}\n\\right]$$\n\n. . .\n\nWe can pre-multiply (put the vector on the left side) too:\n\n$$\\require{enclose}\n\\mathbf{x}^T A \\mathbf{x}\n=\n\\enclose{circle}[mathcolor=\"red\",padding=\"4px\"]{\\begin{bmatrix}2 & 1\\end{bmatrix}}\n\\begin{bmatrix}\n3 & 1\\\\\n1 & 2\n\\end{bmatrix}\n\\begin{bmatrix}2\\\\1\\end{bmatrix}\n=\n\\begin{bmatrix}2 & 1\\end{bmatrix}\n\\begin{bmatrix}7\\\\4\\end{bmatrix}\n= 18$$\n\nThis pattern is called a **quadratic form**. It takes a direction $\\mathbf{x}$ and turns it into one number.\n\n## Identity Matrix\n\nThe identity matrix has 1s on its diagonals and 0s in the off-diagonal entries.\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nidentity <- diag(1, nrow = 2)\nidentity\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n     [,1] [,2]\n[1,]    1    0\n[2,]    0    1\n```\n\n\n:::\n\n```{.r .cell-code}\nA %*% identity == A\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n     [,1] [,2]\n[1,] TRUE TRUE\n[2,] TRUE TRUE\n```\n\n\n:::\n:::\n\n\n\n\n\n. . .\n\nThe identity matrix is the matrix version of multiplying by 1:\n\n$$A I = A$$\n\nIt is useful as a baseline: some matrices stretch, rotate, or shear vectors; the identity leaves them alone.\n\n## Positive definite\n\nA symmetric matrix $A$ is **positive definite** if\n\n$$\\mathbf{x}^T A \\mathbf{x} > 0\n\\quad \\text{for every nonzero vector } \\mathbf{x}.$$\n\n. . .\n\nIntuition:\n\n- $\\mathbf{x}^T A \\mathbf{x}$ acts like a squared length after $A$ has reshaped space\n- positive definite means every nonzero direction has positive length\n- no direction is flattened to zero\n- no direction has a \"negative squared length\"\n\n. . .\n\nFor covariance matrices, positive definite is the condition that let's us visualize it as an ellipse instead of a line, or point (if positive semi-definite).\n\n## Covariance matrix\n\nA covariance matrix stores how variables vary together.\n\nFor two variables:\n\n$$A =\n\\begin{bmatrix}\n\\mathrm{var}(x_1) & \\mathrm{cov}(x_1, x_2)\\\\\n\\mathrm{cov}(x_1, x_2) & \\mathrm{var}(x_2)\n\\end{bmatrix}$$\n\n. . .\n\n::: panel-tabset\n## Read it\n\nRead it geometrically:\n\n- large variance in $x_1$: wide horizontal spread\n- large variance in $x_2$: wide vertical spread\n- positive covariance: the cloud tilts up and to the right\n- negative covariance: the cloud tilts down and to the right\n\n## Positive covariance\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(1)\nA <- matrix(c(1, .5, .5, 1), nrow = 2) \npoints <- mvtnorm::rmvnorm(100, mean = c(0,0), sigma = A)\nplot(points, asp = 1)\n```\n\n::: {.cell-output-display}\n![](lec-dimension-reduction_files/figure-html/unnamed-chunk-4-1.png){width=672}\n:::\n:::\n\n\n\n\n\n## Negative covariance\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(1)\nA <- matrix(c(1, -.5, -.5, 1), nrow = 2) \npoints <- mvtnorm::rmvnorm(100, mean = c(0,0), sigma = A)\nplot(points, asp = 1)\n```\n\n::: {.cell-output-display}\n![](lec-dimension-reduction_files/figure-html/unnamed-chunk-5-1.png){width=672}\n:::\n:::\n\n\n\n\n\n## Zero covariance\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(1)\nA <- matrix(c(1, 0, 0, 1), nrow = 2) \npoints <- mvtnorm::rmvnorm(100, mean = c(0,0), sigma = A)\nplot(points, asp = 1)\n```\n\n::: {.cell-output-display}\n![](lec-dimension-reduction_files/figure-html/unnamed-chunk-6-1.png){width=672}\n:::\n:::\n\n\n\n\n:::\n\n## Visualizing a covariance matrix\n\nA covariance matrix $A$ can be visualized by the points that have the same \"covariance distance\" from the center:\n\n$$\\mathbf{x}^T A^{-1} \\mathbf{x} = c$$\n\nIn two dimensions, that equation draws an ellipse.\n\n::: panel-tabset\n### Plot\n\n\n\n\n\n::: {.cell}\n::: {.cell-output-display}\n![](lec-dimension-reduction_files/figure-html/unnamed-chunk-7-1.png){width=624}\n:::\n:::\n\n\n\n\n\n### Code\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nA <- matrix(c(4, 1.4,\n              1.4, 1), nrow = 2)\n\nlevel <- 4\ntheta <- seq(0, 2 * pi, length.out = 300)\ncircle <- rbind(cos(theta), sin(theta))\n\neig <- eigen(A)\nellipse <- t(eig$vectors %*% diag(sqrt(level * eig$values)) %*% circle)\n\nlim <- max(abs(ellipse)) * 1.15\nplot(ellipse, type = \"l\", asp = 1,\n     xlim = c(-lim, lim), ylim = c(-lim, lim),\n     xlab = expression(x[1]), ylab = expression(x[2]),\n     main = expression(x^T * A^{-1} * x == c))\nabline(h = 0, v = 0, col = \"gray80\")\n\ncols <- c(\"#D55E00\", \"#0072B2\")\nfor (j in 1:2) {\n  axis_vec <- eig$vectors[, j] * sqrt(level * eig$values[j])\n  arrows(0, 0, axis_vec[1], axis_vec[2],\n         length = 0.10, lwd = 3, col = cols[j])\n  arrows(0, 0, -axis_vec[1], -axis_vec[2],\n         length = 0.10, lwd = 3, col = cols[j])\n}\nlegend(\"topright\", legend = c(\"large spread\", \"small spread\"),\n       col = cols, lwd = 3, bty = \"n\")\n```\n:::\n\n\n\n\n:::\n\n## Eigenvalues and Eigenvectors\n\nSome special directions do not get turned by a matrix. They only get stretched or shrunk.\n\n$$A \\mathbf{v} = \\lambda \\mathbf{v}$$\n\n- $\\mathbf{v}$ is an **eigenvector**: a direction that survives the transformation\n- $\\lambda$ is an **eigenvalue**: how much that direction is stretched\n\n. . .\n\nFor a covariance ellipse:\n\n- eigenvectors point along the long and short axes\n- eigenvalues measure spread along those axes\n- the largest eigenvalue points to the direction with the most variation\n\n# Principal Component Analysis\n\n## Intuition\n\nPCA asks:\n\n> Can we rotate the coordinate system so the first axis catches as much variation as possible?\n\n. . .\n\nThe answer comes from the covariance matrix:\n\n- PC1 is the eigenvector with the largest eigenvalue\n- PC2 is the next eigenvector, at a right angle to PC1\n- the PC scores are the data's coordinates in this rotated system\n\nDimension reduction happens when we keep only the first few PC scores.\n\n## In practice\n\nFor a data matrix $X$ with rows as observations and columns as variables:\n\n1.  center each column\n2.  often scale columns to comparable units\n3.  compute the covariance matrix\n4.  find eigenvectors and eigenvalues\n5.  rotate the data onto those eigenvectors\n6.  keep the first few rotated columns\n\n. . .\n\nIn R:\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\npca <- prcomp(X, center = TRUE, scale. = TRUE)\npca$x        # principal component scores\npca$rotation # principal component directions\n```\n:::\n\n\n\n\n\n## Example\n\n### Example 1: two columns that are almost one column\n\nSuppose students with high coding scores also tend to have high statistics scores.\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntoy <- data.frame(\n  coding = c(1, 2, 3, 4, 5, 6),\n  stats = c(1.1, 1.9, 3.2, 3.8, 5.1, 5.9)\n)\n\npca_toy <- prcomp(toy, scale. = TRUE)\nround(summary(pca_toy)$importance, 3)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n                         PC1   PC2\nStandard deviation     1.413 0.057\nProportion of Variance 0.998 0.002\nCumulative Proportion  0.998 1.000\n```\n\n\n:::\n:::\n\n\n\n\n\n. . .\n\nPC1 is almost the average of the two standardized columns:\n\n$$\\mathrm{PC1} \\approx .71(\\text{coding}) + .71(\\text{stats})$$\n\nSo the two-column data set can be summarized by one \"overall performance\" axis with very little loss.\n\n## Example 2: genes mirror geography\n\n::::: columns\n::: {.column width=\"53%\"}\n![](https://upload.wikimedia.org/wikipedia/commons/a/a3/Genes_mirror.jpg){fig-align=\"center\" width=\"100%\"}\n:::\n\n::: {.column width=\"47%\"}\nNovembre et al. (2008) used PCA on European genetic data.\n\n. . .\n\n- rows: people\n- columns: genetic markers\n- PC1 and PC2: two summary coordinates\n\n. . .\n\nThe striking part: the first two PCs resemble a map of Europe, even though PCA was only told about genetic variation.\n\n. . .\n\nGeometric lesson: the leading eigenvectors of a covariance matrix can reveal real low-dimensional structure.\n\nImage: Wikimedia Commons version of Novembre et al., *Nature* 456:98-101.\n:::\n:::::\n\n# Multi-dimensional scaling (MDS)\n\n## Goal of MDS\n\n- The goal of MDS is to represent $q$ objects (data points) in $p$ dimensions.\n\n- Common target dimensions are $p = 2$ or $p = 3$ for visualization purposes.\n\n- MDS requires that for every pair of data points $(i; j) \\in (1, \\ldots, q; 1, \\ldots, q)$, we observe a dissimilarity measure (often distance) $d_{ij} = d_{ji}$.\n\n- The goal of MDS is to estimate $p \\times q$ parameter matrix $\\mathbf{X}$ where the $i$th column $\\mathbf{x}_i$ is estimated by minimizing the function\n\n$$f(\\mathbf{X}) = \\sum_{1 \\leq i \\leq j \\leq q} w_{ij} (d_{ij} - ||\\mathbf{x}_i - \\mathbf{x}_j||)^2$$\n\nwhere $w_{ij} = w_{ji}$ is a nonnegative weight and $||\\cdot||$ is an arbitrary norm, often taken to be the Euclidean ($L_2$) norm.\n\n. . .\n\nPlain-English version:\n\n- PCA starts with variables and finds useful axes\n- MDS starts with distances and tries to draw a map\n- if two objects are similar, MDS tries to place them close together\n- if two objects are dissimilar, MDS tries to place them far apart\n\n## Properties of the objective\n\nThe function\n\n$$f(\\mathbf{X}) = \\sum_{1 \\leq i \\leq j \\leq q} w_{ij} (d_{ij} - ||\\mathbf{x}_i - \\mathbf{x}_j||)^2$$\n\nis\n\n- nonnegative: it adds up squared errors\n- zero only when the displayed distances exactly match the observed distances\n- unchanged if we rotate or flip the whole final picture\n- usually minimized by an iterative algorithm rather than by hand\n\n. . .\n\nMDS is another form of dimension reduction: it trades a large table of pairwise distances for a low-dimensional picture.\n\n<!-- # MDS (an MM solution) -->\n",
    "supporting": [
      "lec-dimension-reduction_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}